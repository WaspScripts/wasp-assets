name: Update Dumps

on:
  workflow_dispatch:
    inputs:
      cache_version:
        description: 'OpenRS2 cache version to process'
        required: true
        type: string
  workflow_call:
    inputs:
      cache_version:
        description: 'OpenRS2 cache version to process'
        required: true
        type: string

run-name: Update Dumps for OpenRS2 cache version ${{ inputs.cache_version }}

jobs:
  build-runelite:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout RuneLite repository
        uses: actions/checkout@v5
        with:
          repository: 'Torwent/runelite'
          ref: 'main'
          path: ./runelite
      - name: Setup Java 11
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '11'
          cache: 'maven'
      - name: Build RuneLite (cache)
        run: |
          cd runelite/cache
          mvn -q -DskipTests package
      - name: Build RuneLite (client)
        run: |
          cd runelite/runelite-client
          mvn -q -DskipTests package
      - name: Copy to staging
        run: |
          mkdir -p staging
          cp runelite/cache/target/cache-*-SNAPSHOT-jar-with-dependencies.jar staging/
          cp runelite/runelite-client/target/*-shaded.jar staging/
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: runelite-jars
          path: staging/

  fetch-openrs2:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch latest OpenRS2 keys.json
        run: |
          mkdir -p cache/input/${{ inputs.cache_version }}
          curl -L -o cache/input/${{ inputs.cache_version }}/${{ inputs.cache_version }}.json https://archive.openrs2.org/caches/runescape/${{ inputs.cache_version }}/keys.json
      - name: Restore/OpenRS2 cache
        uses: actions/cache@v4
        id: openrs2-cache
        with:
          path: cache/input/${{ inputs.cache_version }}
          key: openrs2-cache-${{ inputs.cache_version }}-${{ hashFiles('**/*.json') }}
          enableCrossOsArchive: true
      - name: Download OpenRS2 (miss)
        if: steps.openrs2-cache.outputs.cache-hit != 'true'
        run: |
          mkdir -p cache/input/${{ inputs.cache_version }}
          curl -L -o disk.zip https://archive.openrs2.org/caches/runescape/${{ inputs.cache_version }}/disk.zip
          unzip -o disk.zip -d cache/input/${{ inputs.cache_version }}
    outputs:
      cache-key: openrs2-cache-${{ inputs.cache_version }}-

  dump-itemfinder:
    runs-on: windows-latest
    needs: [build-runelite, fetch-openrs2]
    steps:
      - name: Checkout RuneLite repository
        uses: actions/checkout@v5
      - name: Setup Java 11
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '11'
      - name: Download RuneLite artifacts
        uses: actions/download-artifact@v4
        with:
          name: runelite-jars
          path: runelite-jars
      - name: Restore OpenRS2 cache
        uses: actions/cache/restore@v4
        with:
          path: cache/input/${{ inputs.cache_version }}
          key: openrs2-cache-${{ inputs.cache_version }}-
          enableCrossOsArchive: true
          fail-on-cache-miss: true
      - name: Copy cache
        run: Move-Item "cache/input/${{ inputs.cache_version }}/*" $env:USERPROFILE\.runelite\jagexcache\oldschool\LIVE
      - name: Copy preferences.dat
        run: |
          $runeliteCacheDir = "$env:USERPROFILE\.runelite\jagexcache\oldschool\LIVE"
          New-Item -ItemType Directory -Force -Path $runeliteCacheDir | Out-Null
          Copy-Item -Recurse -Force "tools/preferences/*.dat" $runeliteCacheDir
      - name: Run RuneLite (java)
        run: |
          $jar = Get-ChildItem runelite-jars -Filter "*-shaded.jar" | Select-Object -First 1
          if (-not $jar) { Write-Error "Shaded jar not found"; exit 1 }
          $process = Start-Process -FilePath "java" -ArgumentList "-Dfile.encoding=UTF8 -jar", $jar.FullName -NoNewWindow -PassThru -RedirectStandardOutput "output.log" -RedirectStandardError "error.log"
          $timeout = 300
          $elapsed = 0
          while (-not $process.HasExited -and $elapsed -lt $timeout) {
            Start-Sleep -Seconds 5
            $elapsed += 5
            if (Test-Path "output.log") {
              $content = Get-Content "output.log" -Raw
              if ($content -match "ItemFinder completed") {
                Write-Host "ItemFinder completed detected, stopping process"
                Stop-Process -Id $process.Id -Force
                break
              }
            }
          }
          if ($elapsed -ge $timeout) {
            Write-Host "Timeout reached, stopping process"
            Stop-Process -Id $process.Id -Force
          }
          Get-Content "output.log" -ErrorAction SilentlyContinue
          Get-Content "error.log" -ErrorAction SilentlyContinue
      - name: Upload ItemFinder
        uses: actions/upload-artifact@v4
        with:
          name: itemfinder
          path: itemfinder

  dump-items:
    runs-on: ubuntu-latest
    needs: [build-runelite, fetch-openrs2]
    steps:
      - name: Setup Java 11
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '11'
      - name: Download RuneLite artifacts
        uses: actions/download-artifact@v4
        with:
          name: runelite-jars
          path: staging
      - name: Restore OpenRS2 cache
        uses: actions/cache/restore@v4
        with:
          path: cache/input/${{ inputs.cache_version }}
          key: openrs2-cache-${{ inputs.cache_version }}-
      - name: Run SimbaItemDumper (java)
        run: |
          mkdir -p cache/output
          java -cp staging/cache-*-SNAPSHOT-jar-with-dependencies.jar net.runelite.cache.SimbaItemDumper \
            --cachedir cache/input --cachename ${{ inputs.cache_version }} --outputdir cache/output
          cd cache/output/${{ inputs.cache_version }}/items
          zip -r items.zip ./*.json
      - name: Upload Items
        uses: actions/upload-artifact@v4
        with:
          name: items
          path: cache/output/${{ inputs.cache_version }}/items/items.zip

  dump-gear:
    runs-on: ubuntu-latest
    needs: [build-runelite, fetch-openrs2]
    steps:
      - name: Setup Java 11
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '11'
      - name: Download RuneLite artifacts
        uses: actions/download-artifact@v4
        with:
          name: runelite-jars
          path: staging
      - name: Restore OpenRS2 cache
        uses: actions/cache/restore@v4
        with:
          path: cache/input/${{ inputs.cache_version }}
          key: openrs2-cache-${{ inputs.cache_version }}-
      - name: Run SimbaItemDumper (java)
        run: |
          mkdir -p cache/output
          java -cp staging/cache-*-SNAPSHOT-jar-with-dependencies.jar net.runelite.cache.SimbaGearDumper \
            --cachedir cache/input --cachename ${{ inputs.cache_version }} --outputdir cache/output
      - name: Upload Items
        uses: actions/upload-artifact@v4
        with:
          name: gear
          path: cache/output/${{ inputs.cache_version }}/gear.json

  dump-collisionmap:
    runs-on: ubuntu-latest
    needs: [build-runelite, fetch-openrs2]
    steps:
      - name: Setup Java 11
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '11'
      - name: Download RuneLite artifacts
        uses: actions/download-artifact@v4
        with:
          name: runelite-jars
          path: staging
      - name: Restore OpenRS2 cache
        uses: actions/cache/restore@v4
        with:
          path: cache/input/${{ inputs.cache_version }}
          key: openrs2-cache-${{ inputs.cache_version }}-
      - name: Run SimbaCollisionMapDumper (java)
        run: |
          mkdir -p cache/output
          java -cp staging/cache-*-SNAPSHOT-jar-with-dependencies.jar net.runelite.cache.SimbaCollisionMapDumper \
            --cachedir cache/input --cachename ${{ inputs.cache_version }} --outputdir cache/output
      - name: Upload Collision Maps
        uses: actions/upload-artifact@v4
        with:
          name: collision
          path: cache/output/${{ inputs.cache_version }}

  dump-heightmap:
    runs-on: ubuntu-latest
    needs: [build-runelite, fetch-openrs2]
    steps:
      - name: Setup Java 11
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '11'
      - name: Download RuneLite artifacts
        uses: actions/download-artifact@v4
        with:
          name: runelite-jars
          path: staging
      - name: Restore OpenRS2 cache
        uses: actions/cache/restore@v4
        with:
          path: cache/input/${{ inputs.cache_version }}
          key: openrs2-cache-${{ inputs.cache_version }}-
      - name: Run SimbaHeightMapDumper (java)
        run: |
          mkdir -p cache/output
          java -cp staging/cache-*-SNAPSHOT-jar-with-dependencies.jar net.runelite.cache.SimbaHeightMapDumper \
            --cachedir cache/input --cachename ${{ inputs.cache_version }} --outputdir cache/output
      - name: Upload Height Maps
        uses: actions/upload-artifact@v4
        with:
          name: heightmap
          path: cache/output/${{ inputs.cache_version }}/heightmap.zip

  dump-mapimage:
    runs-on: ubuntu-latest
    needs: [build-runelite, fetch-openrs2]
    steps:
      - name: Setup Java 11
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '11'
      - name: Download RuneLite artifacts
        uses: actions/download-artifact@v4
        with:
          name: runelite-jars
          path: staging
      - name: Restore OpenRS2 cache
        uses: actions/cache/restore@v4
        with:
          path: cache/input/${{ inputs.cache_version }}
          key: openrs2-cache-${{ inputs.cache_version }}-
      - name: Run SimbaMapImageDumper (java)
        run: |
          mkdir -p cache/output
          java -cp staging/cache-*-SNAPSHOT-jar-with-dependencies.jar net.runelite.cache.SimbaMapImageDumper \
            --cachedir cache/input --cachename ${{ inputs.cache_version }} --outputdir cache/output
      - name: Upload Map Images
        uses: actions/upload-artifact@v4
        with:
          name: map
          path: cache/output/${{ inputs.cache_version }}

  dump-npcs:
    runs-on: ubuntu-latest
    needs: [build-runelite, fetch-openrs2]
    steps:
      - name: Setup Java 11
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '11'
      - name: Download RuneLite artifacts
        uses: actions/download-artifact@v4
        with:
          name: runelite-jars
          path: staging
      - name: Restore OpenRS2 cache
        uses: actions/cache/restore@v4
        with:
          path: cache/input/${{ inputs.cache_version }}
          key: openrs2-cache-${{ inputs.cache_version }}-
      - name: Run SimbaNPCDumper (java)
        run: |
          mkdir -p cache/output
          java -cp staging/cache-*-SNAPSHOT-jar-with-dependencies.jar net.runelite.cache.SimbaNPCDumper \
            --cachedir cache/input --cachename ${{ inputs.cache_version }} --outputdir cache/output
      - name: Zip NPCs folder
        run: |
            cd cache/output/${{ inputs.cache_version }}/NPCs
            zip -r npcs.zip .
      - name: Upload NPCs
        uses: actions/upload-artifact@v4
        with:
          name: npcs
          path: cache/output/${{ inputs.cache_version }}/NPCs/npcs.zip

  dump-objectinfo:
    runs-on: ubuntu-latest
    needs: [build-runelite, fetch-openrs2]
    steps:
      - name: Setup Java 11
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '11'
      - name: Download RuneLite artifacts
        uses: actions/download-artifact@v4
        with:
          name: runelite-jars
          path: staging
      - name: Restore OpenRS2 cache
        uses: actions/cache/restore@v4
        with:
          path: cache/input/${{ inputs.cache_version }}
          key: openrs2-cache-${{ inputs.cache_version }}-
      - name: Run SimbaObjectInfoDumper (java)
        run: |
          mkdir -p cache/output
          java -cp staging/cache-*-SNAPSHOT-jar-with-dependencies.jar net.runelite.cache.SimbaObjectInfoDumper \
            --cachedir cache/input --cachename ${{ inputs.cache_version }} --outputdir cache/output
      - name: Upload Object Info
        uses: actions/upload-artifact@v4
        with:
          name: objects
          path: cache/output/${{ inputs.cache_version }}

  dump-sprites:
    runs-on: ubuntu-latest
    needs: [build-runelite, fetch-openrs2]
    steps:
      - name: Setup Java 11
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '11'
      - name: Download RuneLite artifacts
        uses: actions/download-artifact@v4
        with:
          name: runelite-jars
          path: staging
      - name: Restore OpenRS2 cache
        uses: actions/cache/restore@v4
        with:
          path: cache/input/${{ inputs.cache_version }}
          key: openrs2-cache-${{ inputs.cache_version }}-
      - name: Run SimbaSpriteDumper (java)
        run: |
          mkdir -p cache/output
          java -cp staging/cache-*-SNAPSHOT-jar-with-dependencies.jar net.runelite.cache.SimbaSpriteDumper \
            --cachedir cache/input --cachename ${{ inputs.cache_version }} --outputdir cache/output
      - name: Upload Object Info
        uses: actions/upload-artifact@v4
        with:
          name: sprites
          path: cache/output/${{ inputs.cache_version }}

  commit-results:
    runs-on: ubuntu-latest
    needs: [dump-items, dump-gear, dump-collisionmap, dump-heightmap, dump-mapimage, dump-npcs, dump-objectinfo, dump-sprites, dump-itemfinder]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
      - name: Set up Git
        run: |
          git config --global user.name "Wasp Bot"
          git config --global user.email "waspbot@waspcripts.com"
      - name: Download all dump artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
      - name: Organize dump outputs and stage changes
        run: |
          declare -A files=(
            ["artifacts/itemfinder/items-imgs.zip"]="finders/items/items-imgs.zip"
            ["artifacts/itemfinder/id.txt"]="finders/items/data/id.txt"
            ["artifacts/itemfinder/item.txt"]="finders/items/data/item.txt"
            ["artifacts/items/items.zip"]="jsons/item-definitions.zip"
            ["artifacts/gear/gear.json"]="jsons/gear.json"
            ["artifacts/npcs/npcs.zip"]="map/npcs_helper.zip"
            ["artifacts/map/map.zip"]="map/map.zip"
            ["artifacts/objects/objects.zip"]="map/objects.zip"
            ["artifacts/collision/collision.zip"]="map/collision.zip"
            ["artifacts/heightmap/heightmap.zip"]="map/heightmap.zip"
          )
          for src in "${!files[@]}"; do
            dst="${files[$src]}"
            mkdir -p "$(dirname "$dst")"
            if [ ! -f "$dst" ] || ! cmp -s "$src" "$dst"; then
              mv "$src" "$dst"
              git add "$dst"
            fi
          done
          rm -rf artifacts/

      - name: Create Pull Request if changes
        if: steps.commit.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh pr create \
            --base main \
            --head update-${{ inputs.cache_version }} \
            --title "Update cache dumps to version ${{ inputs.cache_version }}" \
            --body "Automated cache dump update from OpenRS2 archive.

            **Cache Version:** ${{ inputs.cache_version }}

            Generated by workflow run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}" \
            --label automated,cache-update
